# Local development example
# Railway will provide PORT and you should add DATABASE_URL in Railway Variables.

# Backend
PORT=5174
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/iaai

# Optional: set to disable SSL locally if needed
# PGSSLMODE=disable

# Mail / SendGrid (if used)
# SENDGRID_API_KEY=
# MAIL_SENDER=
